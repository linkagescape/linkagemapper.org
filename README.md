# linkagemapper.org

**This is not the live site.** linkagemapper.org is published from
the `gh-pages` branch. Edit there. This branch is a 2021 snapshot kept
for history — do not merge it into `gh-pages` (it breaks some things and is also missing some pages).
